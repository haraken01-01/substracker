{
  "name": "substracker-notifications",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 migrations apply substracker-notifications --local",
    "db:remote": "wrangler d1 migrations apply substracker-notifications --remote",
    "test": "node --test"
  },
  "dependencies": {
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "wrangler": "^4.50.0"
  }
}
